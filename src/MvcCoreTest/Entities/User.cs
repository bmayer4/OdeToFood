using Microsoft.AspNetCore.Identity.EntityFrameworkCore;

namespace MvcCoreTest.Entities
{
    /// <summary>
    /// This class isn't necessary unless you plan to 
    /// add additional code. However, everything that 
    /// is needed is probably already built into IdentityUser.
    /// Check the class implementation first before adding 
    /// code. 
    /// </summary>
    public class User : IdentityUser
    {
        
    }
}